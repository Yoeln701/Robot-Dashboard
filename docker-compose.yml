version: '3'

services:
  robot:
    image: ghcr.io/francescodelduchetto/cmp9134_2526_robotsim:latest
    ports:
      - "5001:5000"

  dashboard:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - robot